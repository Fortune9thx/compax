# 07 · ASSET REQUIREMENTS LIST

Per project rule: **sourced assets are always requested from the owner — never invented.**
Items marked `[ASK]` block their feature until provided or explicitly waived.
Items marked `[CODE]` I can build in SVG/CSS/Framer without external assets.

## Blocking — needed from you

1. `[ASK]` **Compass logomark + wordmark** — SVG. Used in nav, landing, favicon.
   (If none exists yet: say "design one in code" and I'll build a geometric SVG mark,
   or commission one externally.)
2. `[ASK]` **Display font license decision** — Kastle uses *Reckless Neue* (paid,
   Displaay Type). Options:
   a) Buy Reckless Neue (closest to reference)
   b) Use free **Instrument Serif** from Google Fonts (my default if you say "free")
   c) Another serif you prefer (e.g. *Editorial New*, also paid)
3. `[ASK]` **Landing hero centerpiece** — Kastle/Sui use a signature 3D orb / Lottie
   animation. Options:
   a) I build a pure-SVG/canvas animated "compass rose of flowing capital" `[CODE]`
   b) You source a Lottie/3D asset (LottieFiles, or commissioned)
   Decide a or b.

## Nice-to-have — can ship without

4. `[ASK]` Agent avatars for the four council members (Risk/Yield/Liquidity/Macro).
   Default: colored identity dots per design language `[CODE]`.
5. `[ASK]` Partner/chain logos for landing credibility strip (GenLayer logo at minimum —
   grab from genlayer.com brand kit with their permission).
6. `[ASK]` Open Graph / social share image (1200×630).

## Built in code — no external assets needed

- Capital-flow particle system (SVG + Framer Motion) `[CODE]`
- Ecosystem living map nodes/edges `[CODE]`
- Reputation ring, allocation donut, tension bars, charts `[CODE]`
- All iconography (Lucide) `[CODE]`
- Ambient dotted flow textures `[CODE]`
- JetBrains Mono + Instrument Serif via next/font (Google) `[CODE]` — pending #2

## Current answers needed
Reply with: **#2 (a/b/c)** and **#3 (a/b)** — everything else can proceed.
